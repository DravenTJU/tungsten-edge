@AGENTS.md

## Obsidian 库

**笔记库**：`/Users/caye/Documents/Obsidian Vault/Projects/macos-dock-cc-v2/`。协作规则见全局 CLAUDE.md，但本项目有下面这套覆盖。

### 本项目覆盖（2026-08-01 owner 定，优先于全局规则）

owner 侧只剩两个入口，**其余笔记都已归档**：

| | 位置 | 谁写 |
|---|---|---|
| **进度看板**（唯一进度/待办数据源） | 笔记库根目录 `进度看板.html` | agent |
| **灵感速记**（owner 唯一手写入口） | `03 待办与想法/灵感速记.md` | owner 写，agent 只读 |

- **「打检查点」在本项目只写 `进度看板.html` 一处**——已实测验收的功能、新待办、状态变化都进它。原 `00 当前进度.md` 与 28 张 md 待办卡已于 2026-08-01 归档到 `Archive/2026-08-01 看板迁移/`，**不要再写它们**。
- **改看板的操作规矩**：内容全在文件内 `<script id="board-data">` 的 JSON 块里，**改内容只动 JSON，不碰布局代码**；改之前先把旧版复制到 `Archive/看板快照/进度看板-YYYY-MM-DD.html`（笔记库不在 git，这是唯一的后悔药）。卡片支持 `#card=<卡名>` 深链，owner 可能直接把这种链接发过来。布局代码里现在还带着「🔖 标记待整理」这套交互（见下条），改它属于功能改动，得单独说清楚、单独快照。
- **「整理待办」流程（2026-08-01 加）**：owner 在看板卡片上点 🔖 打标记（标记只存在他浏览器的 localStorage，agent 读不到），攒够了点顶栏「复制给 Claude」，粘一段口令过来——**看到这段口令，或 owner 说「整理待办 / 看标记」，就执行**：
    1. 逐张读卡片正文，**核实现状再下结论**（查提交、读代码、对 `Docs/`），三选一：✅ 其实已经做完了（附证据）／⛔ 前提不存在了或与现在的产品方向冲突／⏳ 仍然有效、值得留。不许凭印象判。
    2. **先报告，一律不动看板**。owner 点头之后才写：先快照 → 销掉的卡从 `cards` 移除并在底部「已完成卡片核对」表补一行（提交号栏：真做完了填提交号，取消的填 `—`；备注写「取消：理由」）→ 判「留着」的卡在 JSON 顶层 `marksResolved` 里写 `"卡名": "<当前时间 ISO>"` → `meta.updated` 改今天。
    3. `marksResolved` 是给前端自动清标记用的（卡被销掉、或评估时间晚于打标记时间 → 标记自动消失）。**别删这个字段，也别手工清空它**。
- **产品决策与理由** → 仓库 `Docs/27-product-decisions.md`（agent 自动维护，达成决策就写，不等口令、不写进笔记库）
- **工作方式踩坑** → 仓库 `Docs/28-process-pitfalls.md`（同上）
- 全局规则里「决策写进设计决策笔记」那条**在本项目不适用**；「整理灵感速记」照旧（采纳的条目直接加进看板，不再建 md 卡片）。
- `Assets/协作文档地图.*` 是一次性产物，owner 已看过，**不再更新**。
